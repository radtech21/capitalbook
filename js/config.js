// Point the frontend at your Capital Book API.
// - Local dev: the API runs on http://localhost:4000 (default below).
// - Same-origin deploy (frontend served by the API host): set this to ''.
// - Separate hosts: set this to your API's base URL, e.g. 'https://api.yourfirm.com'.
// window.CB_API = 'http://localhost:9091';
window.CB_API = 'https://api.capitalbook.ca';
