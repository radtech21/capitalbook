# Capital Book — Frontend

Static frontend (landing page + app) for the Capital Book CRM. Plain HTML/CSS/JS, served by `server.js`.

The API lives in [`../backend`](../backend) of this same repository.

## Run locally

```
npm install
npm start
```

Configure the API base URL in `js/config.js`.
